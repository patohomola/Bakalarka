Nasledne som  spustil Klastrovaci nastroj s takymto nastavenim:
\begin{lstlisting}[language=bash, caption={Running Clustering Tool}, label={lst:running-clustering-tool}]
    python -m clover.main  -I output.txt -O example.txt -L 150 -P 2 --no-tag
\end{lstlisting}
